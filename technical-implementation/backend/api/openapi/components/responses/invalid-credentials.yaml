description: The supplied login credentials were rejected.
content:
  application/json:
    schema:
      $ref: ../schemas/api-error.yaml
    example:
      error:
        code: AUTH_INVALID_CREDENTIALS
        message: The provided email or password is incorrect.
